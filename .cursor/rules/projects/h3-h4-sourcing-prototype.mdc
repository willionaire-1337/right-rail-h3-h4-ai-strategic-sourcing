---
description: Living notebook for the H3/H4 AI strategic sourcing prototype
alwaysApply: false
---

# H3/H4 AI Strategic Sourcing Prototype — notebook

## Current status

- Next.js 16 prototype of AI-assisted supplier discovery for Stamping Services,
  styled with the vendored Tailoft design system (`public/tailoft/`).
- Supplier results render as full-width cards (`supplier-card.tsx`); a two-up
  tile variant (`supplier-tile.tsx`) exists but is not currently mounted.

## Key decisions

- 2026-08-20 (William Johnson, via agent): Added a "Contact Supplier" secondary
  CTA to the left of "Visit Website" on supplier cards and tiles. It opens
  `contact-supplier-modal.tsx`: a "Contact {Supplier}" dialog with a General
  Inquiry / Request Quote toggle. General Inquiry carries an inquiry-type
  dropdown; Request Quote carries description/quantity/date fields. Submission
  is simulated (confirmation state only) — no backend.
- Modals follow the existing scrim + card pattern (`gate-scrim`); forms use
  plain `<fieldset>` markup so Tailoft styles labels/inputs natively.
- 2026-08-20 (William Johnson, mock provided): Contact Supplier CTA styled as a
  tinted secondary — blue-10 fill, blue-100 text, no border, solid envelope
  icon. Tailoft's icon map lacks an envelope, so `globals.css` maps
  `l-icon[name="envelope"]` to the Font Awesome glyph (f0e0) the vendored
  fonts already carry.
- 2026-08-20 (William Johnson): Multi-recipient quote requests show a
  "Sending to" row below Estimated quantity / Response needed by: gray-outline
  pills, each with a mini monogram tile (suppliers have no image logos) plus
  the name. Hidden for single-recipient sends. The old top-of-dialog chip row
  was removed.
- 2026-08-20 (William Johnson): The Project name field is auto-seeded by
  `buildProjectName` — first material + first process + part, title-styled
  (e.g. "Aluminum Progressive Die Brackets"). No recipient in the name (was
  briefly "… — NN, Inc.", removed same day). Editable defaultValue, same
  no-AI template rules.
- 2026-08-20 (William Johnson): The contact dialog is quote-only — the General
  Inquiry tab was removed. The project description is pre-drafted from logged
  answers by `buildProjectDescription` (pure template rules, no AI; bracket
  use case hardcodes "brackets" unless a Part type answer exists). The
  requirement pills moved into a closed-by-default Tailoft
  `details kind="accordion"` labeled "Your requirements (n)".
- 2026-08-20 (William Johnson): The quote form has an Attachments module built
  from Tailoft's own `l-fileupload` (dashed dropzone, shows accepted types) and
  `l-filepreview` (name/size/remove slots). Files live in modal state only —
  prototype, nothing uploads. Accepted: .pdf, .step, .iges, .dxf, .dwg, .zip.
- 2026-08-20 (William Johnson): The contact dialog addresses multiple
  suppliers — `SupplierResults` owns the single modal instance and passes the
  recipient set (clicked card's supplier + all selected). Multi-recipient view
  titles "Contact N Suppliers" with a "Sending to" chip list. The selection
  bar's Request Quote button opens the same dialog on the quote tab.
- 2026-08-31 (William Johnson): Unanswered quote requests get two follow-up
  emails, both Gmail pages sharing `SupplierQuoteEmail` with the original:
  `/supplier-email/reminder` (next day — "we haven't heard back") and
  `/supplier-email/final-notice` (day 2 — if they still don't reply, the
  buyer's RFI is sent to other suppliers). Same recap, CTAs, and Not
  interested → buyer-notification path as the original. The sent confirmation
  lists all three previews. Inbox unread count is 1 / 2 / 3 across the
  sequence.
- 2026-08-20 (William Johnson): The supplier-facing email lives on its own
  Gmail-style page at `/supplier-email` (was an inline preview in the dialog).
  Submitting the quote form stashes a `QuoteEmailPayload` in localStorage
  (`supplier-email-preview`), opens the page in a new tab, and flips the
  dialog to a compact "Quote request sent" confirmation with a "View supplier
  email" link. **Superseded 2026-08-31: the confirmation now lists Original
  request / Reminder / Final notice (see the 2026-08-31 follow-up-emails
  entry).** The page renders full Gmail chrome (topbar, search, compose,
  rail, subject + Inbox chip, sender row from Thomas <quotes@thomasnet.com>,
  Reply/Forward). Email body: buyer persona (Sarah Mitchell, Meridian
  Dynamics), light-blue quoted project description, requirements list,
  full-width "Not interested" / "View contact details" CTAs, reply-directly
  note, and a light-blue blue-text "Sent via Thomas on behalf of…" footer.
  Body copy says "through Thomas" (was Xometry). For multi-supplier sends the
  page shows the copy addressed to the first recipient. Clicking "Not
  interested" opens `/buyer-email` (buyer Gmail: that supplier declined) and
  leaves in-page links to both `/buyer-email` (messaging only) and
  `/buyer-email-matches` (same copy plus 5 other catalog suppliers, skipping
  anyone already contacted). Shared Gmail chrome lives in `gmail-shell.tsx`.
- 2026-08-20 (William Johnson): Request Quote form fields are Project name,
  Project description, a "Your requirements" chip list echoing logged
  left-rail answers (threaded from `SupplierResults` via the card), Estimated
  quantity, Response needed by, and a pre-checked required consent checkbox
  ("…agree to share your project and contact details") in place of the
  "sent directly" note.
- 2026-08-20 (William Johnson): The contact dialog's submit button is labeled
  "Send Inquiry" (was "Request Quote"). The results-footer button that opens
  the dialog still says "Request Quote".
- 2026-08-20 (William Johnson): A floating "Quick Contact" CTA (white pill,
  blue outline/text, sparkles icon) docks sticky to the bottom-right of the
  results list, above the Exit Agent pill. Clicking it opens the contact
  dialog preloaded with the top 10 ranked suppliers (`QUICK_CONTACT_COUNT`),
  bypassing the 5-supplier manual selection.

- 2026-08-27 (William Johnson): This `right-rail-*` clone adds a "Select
  Suppliers" rail (`select-suppliers-rail.tsx`) to the right of the results
  list: the top 3 ranked suppliers by name, an "Add to shortlist" secondary
  CTA (saves them), and a "Send RFI to Suppliers" primary CTA (opens the
  contact modal addressed to those 3). Hidden at ≤1024px.
- 2026-08-27 (William Johnson): Supplier cards are separated rounded panels —
  1px border, 8px radius, 12px gap and padding (was flush cards on a
  hairline-gap gray backdrop).
- 2026-08-27 (William Johnson): Cards dropped the "Contact Supplier" and
  "Select" actions; only Save and Visit Website remain. Contact flows now run
  through the rail RFI, the selection footer, and Auto Contact.

- 2026-08-27 (William Johnson): The rail is titled "Shortlist or Contact
  Suppliers" (was "Select Suppliers"). Cards swapped the Save bookmark for a
  "+ Add" secondary CTA left of Visit Website that toggles the supplier on/off
  the rail list ("✓ Added" when on). **Superseded 2026-08-31: the toggle now
  reads "+ Add to List" / "✓ Added to List".** The rail auto-seeds with the top 3, which
  are also deselectable — dismissals are tracked so they stay off; re-adding
  restores them. Manual additions append in click order.
- 2026-08-27 (William Johnson): The capability pills row on cards became a
  requirements summary: green circle-check +   "All X requirements met" (exact
  matches), "Y of X requirements met" (near matches, one answer relaxed), or
  "Matches your search" before any answers. `CapabilityRow` still exists for
  the unmounted tile variant. **Superseded 2026-08-31: current wording is
  "All X requirements met" (2+), "Requirement met" (exactly 1), "Matches your
  search" (0), shown on every card including near matches — see the 08-28
  boss-feedback entry below for the intermediate exact-only state.**
  **Superseded 2026-08-31 (later): the green requirements badge is gone —
  cards show up to 5 grey-outline blue-check capability pills (`matchPillsFor`
  in `lib/match-pills.ts`; see the same-day match-pills entry).**

- 2026-08-27 (William Johnson, reference HTML "Supplier Search 2a — responsive
  standalone" provided): Full layout rework to the reference's three-stage
  design, keeping Tailoft tokens/icons and the Canela display font.
  - Stage bar under the search header: 1 DEFINE ("N of M" questions) /
    2 EVALUATE ("X of 2,482 match") / 3 ENGAGE ("N selected"), columns sized
    to the panes below (280 / flex / 320).
  - Fixed-width define pane (280px desktop, 240px ≤1100px) — the drag divider
    and resizable split were removed. Pane restyled flat: 30px badge + serif
    15px header, hairline-separated question sections, single-column radio
    options, settled answers as tinted "✓ answer / Edit" pills, footer toolbar
    « / ← Back / Skip → / ↺ / ≡. The mobile bottom-sheet + swipe grip were
    removed.
  - Center column: title row carries a 230px location field (wired to the loc
    answer) + All Filters; answer chips are compact single pills with the
    label inside (PROCESS Progressive Die ✕). Cards are flat rows separated
    by hairlines (supersedes the rounded-panel look from earlier today):
    46px bordered monogram, dot-joined facts line ("City, ST ZIP · N
    employees · $rev · Est. year"), semibold types line, description with
    more+, and the requirements summary as a bordered pill with a solid green
    circle check. Card CTAs are 36px.
    **Superseded 2026-08-31: the green summary pill is replaced by grey-outline
    blue-check capability pills (max 5) — see the same-day match-pills entry.**
  - Engage rail (320px): "Shortlist & contact suppliers" header with
    paper-plane badge and subcopy, removable entries (28px monogram, name,
    location, ✕), "+ Add to shortlist", "Send RFI to N Suppliers", and the
    Auto Contact CTA moved in from the floating dock (dashed tinted button).
    The floating Quick Contact pill and the dark selection footer were
    removed; `SupplierResults` reports the rail count up via
    `onRailCountChange` for the stage bar.
  - Responsive per the reference: ≤1100px hides the rail and shows a fixed
    bottom engage tray (avatar stack, "N suppliers to engage", Send RFI);
    ≤699px hides the stage bar/nav links/location for mobile tabs
    (1 Define / 2 Evaluate / 3 Engage · N) that swap the single column, a
    ✦ Refine tray button jumping back to Define, and a compact card variant
    (38px logo, +Add only, Visit Website as a foot-row link, media hidden).

- 2026-08-27 (William Johnson, follow-ups): results header scoped to the
  center column only (no longer runs over the engage rail — header + list now
  wrapped in `.results-center` beside the rail); define pane header retitled
  "Smart filter your search"; define option rows bumped to 13px text.

- 2026-08-27 (William Johnson, later tweaks): type sizes — rail CTAs 15px,
  define question titles 14px, option rows + settled answer pill 13px. Answer
  chips moved out of the results header into the All Filters drawer (pinned
  under the actions row), category labels dropped, pills ~8px thinner; the
  results header now carries its own bottom hairline. Engage rail picks
  restyled from hairline rows to compact tinted pills (22px monogram,
  tight name/location stack, hairline-separated ✕).

- 2026-08-27 (William Johnson, engage rail v2 + polish pass): rail redesigned
  around a drafted RFI — header now counts "N suppliers saved", picks are
  white grey-outlined name-only chips flowing left-to-right (no truncation),
  then an "RFI drafted for you" card (green flag pill, headline synthesized
  from material + process + part answers, "Written from your N logged
  requirements · …", Preview RFI →), "Contact N Suppliers" (**superseded
  2026-08-28: was "Send to N Suppliers"**), and quiet
  sub-CTAs "+ Add to shortlist" / "Visit Website(s)". Auto Contact and the
  "+ Add" chip were dropped. Location question removed from the define flow
  (header field / drawer only; the answer still filters). Card fact rows got
  their icons back (location-dot/users/landmark/calendar/industry) in dark
  text; requirements badge tinted green. Stage bar: define count dropped,
  match note sits beside EVALUATE with the count blue. Results header sheds
  the location field when the center column is narrow (container query).
  Done-card CTAs stack full-width; assorted type-size tweaks. Deployed to
  GitHub Pages via the repo workflow.

- 2026-08-28 (boss feedback round, via William Johnson): desktop stage bar
  (1 DEFINE / 2 EVALUATE / 3 ENGAGE) removed — the columns speak for
  themselves; the phone tab strip stays since it's real navigation. The
  "N of M requirements met" pill read as probabilistic scoring, so cards now
  only badge "All N requirements met" (exact matches); near matches carry no
  badge — the list divider naming the relaxed answer is the only callout —
  **Superseded 2026-08-31: every card badges again ("for now", William) —
  "All N requirements met" for 2+, "Requirement met" for exactly 1,
  "Matches your search" for 0; near matches included. Later the same day the
  near-match "Closest matches — … except X" divider was removed too (William),
  so near matches now appear with no callout at all.** —
  **Superseded 2026-08-31 (later): the green requirements badge is gone;
  cards show capability pills instead (see the same-day match-pills entry).** —
  and rail auto-picks now draw exclusively from exact matches so an RFI
  never pre-targets a near match. "Visit Website(s)" sub-CTA dropped (it did
  nothing). Cards compacted (12px vertical padding, 8px gaps, 40px logo,
  32px CTAs, 112×72 media) so multiple results show above the fold. Rail
  chips lost their ✕ — the card's Add/Added toggle is the single
  select/deselect control; chips just reflect it.

- 2026-08-28 (William Johnson): rail no longer auto-seeds — it starts empty
  with a placeholder (circle-plus icon, "Add suppliers to contact or
  shortlist"); the card Add/Added toggle is the only way suppliers get on.
  **Superseded 2026-08-31: the desktop rail no longer starts visible at all —
  it stays hidden until the buyer adds a supplier or logs a smart-filter
  answer (see the 2026-08-31 dormant-rail entry). The placeholder still
  shows once the rail reveals via a smart-filter answer with no adds yet.**
  **Superseded 2026-08-31 (later): the rail is visible on load again — dormant
  hiding was dropped (see the same-day "rail open by default" entry).**
  **Superseded 2026-08-28: the placeholder icon is now the factory/supplier
  glyph (`l-icon name="industry"`, same as card company-type rows), not
  circle-plus.** The RFI card + send/shortlist CTAs are pinned to the rail's
  foot (`margin-top: auto`); the placeholder centers in the leftover space
  above, and saved chips flow from the top with a minimum gap to the card.
- 2026-08-28 (William Johnson): the "All N requirements met" pill is
  hoverable/focusable — a CSS popover ("Matching your search") lists what
  matched with green checks: the searched category (Service · Stamping
  Services) first, then each logged answer with its label. Data threaded as
  `requirementDetails` from `supplier-results.tsx` into `supplier-card.tsx`;
  the category is not counted in the pill's N.
  **Superseded 2026-08-31: the badge, hover popover, and `requirementDetails`
  were removed. Cards show capability pills instead (see match-pills entry).**
- 2026-08-28 (William Johnson): the contact modal's "Sending to" list
  collapses to the first 2 recipient chips plus a tinted "+X" chip
  (`RECIPIENT_PREVIEW` in `contact-supplier-modal.tsx`); clicking +X unfolds
  the full list. Collapse state resets each time the dialog opens.
  Skip moved from the footer toolbar into every question card: a quiet
  always-visible "Skip →" row below the options (sibling to "I don't know");
  the footer slot became a "Done" button that closes the define pane.

- 2026-08-28 (William Johnson): the engage-rail primary CTA reads "Contact N
  Suppliers" (was "Send to N Suppliers"). **Supersedes the 2026-08-27 rail v2
  wording "Send to N Suppliers".** The mobile tray still says "Send RFI".

- 2026-08-28 (William Johnson): the rail's RFI card gets a placeholder state
  when zero smart-filter answers are logged: gray "No RFI drafted yet" flag
  (`.rail-rfi-flag-empty`) plus one line — "Answer the smart filter questions
  and we'll draft an RFI from your requirements." — no title, no Preview RFI
  link. The old zero-answer copy ("RFI drafted for you" / search query /
  "Written from your search") is gone; the drafted card now only appears once
  ≥1 answer is logged. The Contact button still opens the modal regardless.

- 2026-08-31 (William Johnson): the desktop engage rail is visible on load
  again — empty placeholder until a supplier is added, empty RFI card until
  an answer is logged. **Supersedes the same-day dormant hide (below).**

- 2026-08-31 (William Johnson): the desktop engage rail is dormant (hidden,
  results full-width) until the buyer clicks a card's "+ Add" or logs a
  smart-filter answer — `dormant` prop on `SelectSuppliersRail` adds
  `.select-rail-dormant` (`display: none`) when both `railSuppliers` and
  `logged` are empty. CSS class, not unmount, so the ≤699px engage-tab rule
  (higher specificity) still shows the rail on the phone tab; the ≤1100px
  bottom engage tray is untouched.
  **Superseded 2026-08-31: rail is visible by default; `dormant` and
  `.select-rail-dormant` were removed. Phone hide-at-1100 / engage-tab
  behavior is unchanged.**

- 2026-08-31 (William Johnson): card CTA reads "+ Add to List" / "Added to
  List" (was "+ Add" / "Added"). The rail-hiding trigger wording above
  ("clicks a card's + Add") refers to this same button. **Superseded
  2026-08-31: the rail no longer hides, so that trigger is unused.**

- 2026-08-31 (William Johnson): the smart filter now opens with "What product
  are you trying to make?" — the `part` question (Q11) was promoted to the
  head of `ASK_SEQUENCE`, retitled "Product", and flagged `search: true` in
  `questionnaire.json`. Search questions render a typeahead combobox in
  `AskBlock` (same markup/CSS as the location entry, magnifying-glass icon,
  "Set product" submit; since later that day the submit sits full-width
  under the field, compact and styled like the footer Done button — applies
  to the location entry too) over their full option catalog — never pruned by
  `planField` and exempt from the earns-its-place narrowing check, same as
  the location question. Catalog gained buyer-language stamped products
  (Brackets, Washers, Clips, Springs, Terminals, Contacts, Shims, Gaskets,
  Retaining Rings, Busbars, Enclosures, Housings, Shields, Eyelets, Ferrules,
  Grounding Straps, Battery Tabs, Lids & Covers) ahead of the taxonomy rows;
  typed free-text answers are also accepted. Process is now the second
  question.

- 2026-08-31 (William Johnson): the define pane's open/close affordances say
  "Smart Filters", not "agent": the floating pill reads "Open Smart Filters",
  the footer collapse chevron and the done-card primary button read "Close
  Smart Filters" (was "Open Agent" / "Collapse the agent pane" /
  "Close agent").

- 2026-08-31 (William Johnson): every result card badges its requirements
  ("All N requirements met" / "Requirement met" for one / "Matches your
  search" for none), and the near-match "Closest matches — … except X"
  divider was removed — near matches are visually indistinguishable for now.
  See the superseded 08-27/08-28 badge entries above for the history.
  **Superseded 2026-08-31 (later): the green badge is replaced by capability
  pills — see the same-day match-pills entry.**

- 2026-08-31 (William Johnson): the rail's action footer became a
  "Supplier Response Guaranteed" card (`.rail-guarantee`): bordered white
  card, centered, with a green shield-check badge breaking the top border
  (white box-shadow ring masks the border behind it), "Get a response within
  2 days or we'll make it right." copy, then the full-width Contact CTA and
  "+ Add to shortlist" link inside the card. Replaces the bare
  `.select-rail-actions` stack (CSS block removed). **Superseded 2026-08-31
  (same day, William): the guarantee concept was dropped entirely — the
  footer is a plain `.select-rail-actions` stack again, just the full-width
  "Contact N Suppliers" primary CTA and the "+ Add to shortlist" sub-link;
  the `.rail-guarantee*` CSS was removed.**

- 2026-08-31 (William Johnson): the drafted RFI card's subtext is the logged
  spec as stacked plain lines (no bullets) — one "Label: Value" per answer,
  11.5px, label in body color, value semibold dark (`.rail-rfi-spec`;
  `requirementPreview` is now `{label, value}[]` threaded from
  `supplier-results.tsx` using the `RFI_SPEC_LABELS` short-label map).
  Replaces the earlier same-day single-sentence "Written from your N logged
  requirements · …" preview and the brief bulleted-list version.

- 2026-08-31 (William Johnson): the product typeahead opens downward
  (`.location-suggest-down` flips the shared `.location-suggest` from
  `bottom: 100%` to `top: 100%`) — the part question sits at the top of the
  define pane, so upward suggestions were clipped. The location question
  still opens upward since it renders at the transcript foot.

- 2026-08-31 (William Johnson): the RFI card is a module with a full-width
  status bar across its top (`.rail-rfi-bar`, replaces the inline
  `.rail-rfi-flag` pill) that tints with draft progress: white bar / grey
  text + "No RFI drafted yet" at zero answers (`.rail-rfi-empty`), light
  blue / blue text + "RFI drafted for you" from the first answer
  (`.rail-rfi-drafted`), light green / green text at 5+ answers
  (`.rail-rfi-defined`). Body content sits in `.rail-rfi-body` below the bar;
  the module is bordered with the bar clipped by `overflow: hidden`.
  **Superseded 2026-08-31: drafted bar uses the former body blue
  (`blue-5`) and is shorter (`padding: 4px`); body is white. Empty bar
  stays white; 5+ stays green.**
  **Superseded 2026-08-31 (later): the 5+ green state (`.rail-rfi-defined`)
  was dropped — any drafted RFI stays blue. See the same-day RFI-bar-stays-blue
  entry.**

- 2026-08-31 (layout fix): the rail's middle section (`.select-rail-list`
  chips / `.rail-placeholder`) is now the flexible, internally-scrolling
  piece (`flex: 1 1 auto; min-height: 0; overflow-y: auto`), so the RFI and
  guarantee cards at the foot are never pushed below the viewport — they
  were getting clipped on shorter windows.

- 2026-08-31 (William Johnson): buyer decline emails. When a supplier clicks
  "Not interested" on `/supplier-email`, Thomas notifies the buyer (Sarah
  Mitchell) in the same Gmail chrome. Two versions: `/buyer-email` is only
  the decline message; `/buyer-email-matches` appends 5 other suppliers from
  the catalog (excluding anyone on that send) each with a Contact CTA.
  Payload now stashes `supplierId` + `contactedIds` so suggestions don't
  re-offer the declined or already-messaged shops. Gmail chrome extracted to
  `gmail-shell.tsx` and shared across all three inbox pages. **Superseded
  2026-08-31: five inbox pages share the chrome — original, reminder, final
  notice, buyer decline, and buyer decline with matches.**

- 2026-08-31 (William Johnson): buyer decline emails' blue info module
  (`.contact-email-foot`) reads "We recommend contacting 3 or more suppliers
  to get the best chance of receiving a fast response." **Supersedes the
  same-day "Sent via Thomas. We'll keep looking out for suppliers who can
  help with {project}." footer.** Same box on both `/buyer-email` and
  `/buyer-email-matches`.

- 2026-08-31 (William Johnson): buyer decline subject is "{Supplier} is not
  able to complete your contact request" (e.g. Ronson Manufacturing).
  **Supersedes the same-day "{Supplier} isn't interested in {project}"
  subject.** Applies to both buyer-email versions.

- 2026-08-31 (William Johnson): buyer decline body is constructive, not a
  dead end: supplier "isn't able to take on your request… right now," then
  "That's a normal part of sourcing — and it doesn't slow you down," plus a
  full-width "Continue sourcing suppliers" CTA back to the prototype home.
  Matches version intro is "…suppliers you can contact next." **Supersedes
  the same-day "isn't interested in your quote request" / "can't take this
  project on right now" body.**

- 2026-08-31 (William Johnson): define-pane footer Back control is the word
  "Back" only — the leading "←" was dropped. **Supersedes the 2026-08-27
  toolbar notation "← Back".**

- 2026-08-31 (William Johnson): result cards no longer show a green
  "All N requirements met" badge. The foot row is up to 5 grey-outline
  blue-check pills (existing `.cap-pill` style, tighter in `.card-foot`)
  with generic capability labels from `matchPillsFor` — always "Stamping
  Services", then material (Aluminum → "Aluminum Alloy"), process family
  (Progressive Die → "Metal Stampings"), part, mapped features, industry,
  certs. Raw answers, quantities, thickness, location, and the hover
  popover are gone. **Supersedes the 2026-08-27/08-28/08-31 green-badge
  entries.**

- 2026-08-31 (William Johnson): the drafted RFI status bar stays blue
  from the first answer onward — it no longer turns green at 5+ answers.
  `.rail-rfi-defined` was removed. Empty stays white. **Supersedes the
  same-day "5+ stays green" RFI-bar tint.**

## Open threads

- Analytics ask from the boss (2026-08-28): instrument how many buyers edit
  the drafted RFI before sending (draft-changed vs sent-as-is). Prototype has
  no analytics layer; belongs in the production spec.
- Lead-form "additional questions" (beyond the logged requirements echoed on
  the RFI) are out of H4 scope per the boss's own read; nothing to build here
  yet.

- Contact form submissions go nowhere (prototype); wire up if/when a backend
  exists.
- Inquiry-type dropdown options are first-pass ideas — confirm the list with
  design/product.
